# dist/ is intentionally NOT ignored: the package is distributed via git/npx
# (not npmjs), so the compiled CLI must ship with the repo for installs to work
# without a build step.
node_modules/
coverage/
*.log
.DS_Store
.env
.env.*

# Local user state (persisted sessions)
.testmymcp/

# Phase 3 spec source (cloned, gitignored)
spec-src/

# Internal planning / working notes (only BLUEPRINT.md is kept in VCS)
ACCOUNT_FOR.md
PHASE3-HANDOFF.md
PHASE3-SPEC-INVENTORY.md
PHASE4-HANDOFF.md
